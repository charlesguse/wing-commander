# Reference: Current Default Tool Lists Per Stage (FR-013) - superseded

**Feature**: 026-configurable-tool-lists

> **Superseded. Do not edit this file to record a tool-list change.**
>
> The live table is
> [`specs/010-reusable-pipeline/contracts/stage-interfaces.md`](../../010-reusable-pipeline/contracts/stage-interfaces.md).
> That is the copy consumers are pointed at, and the only one
> `.github/scripts/verify-stage-tool-lists.py` (Gate 27) checks against the
> `wing-commander-tool-args` call sites.

This was the planning-time draft of the per-stage default reference FR-013
requires ("a consumer can determine, from documentation alone, what each
stage's default tool lists are" - SC-006). Its table was carried into
`specs/010-reusable-pipeline/contracts/stage-interfaces.md` verbatim at
implementation time (research.md D7), and the two were then expected to be
hand-updated together.

They were not, and nothing checked. This draft never gained the two
`pr-conversation.*` rows added by specs/033, while its own prose went on
referring to a `pr-conversation.act` row it no longer contained - the drift
#147 predicted, already realised, in the document whose entire job is to be
accurate.

The table has been removed from this file rather than repaired. A second
hand-maintained copy that merely happens to be correct today is the same
defect waiting to recur; one copy with a gate on it is the fix. What was
here is preserved in this feature's git history.

## What the draft recorded, for the record

Two notes from this draft that are context rather than data, and that the
live table carries forward in its own words:

- Every default disallowed list carries `ScheduleWakeup`, `Monitor`,
  `SendMessage` - interactive-resume tools a one-shot Action can never
  service, so they stay functionally inert even if a consumer's
  `extra-allowed-tools`/override re-adds them (research.md, "Constitutional
  considerations"). `watchdog.diagnose` is the one exception, and the live
  table footnotes it.

- `implement.cycle`/`implement.retry` no longer carry `Bash(gh run view:*)`/
  `Bash(gh run list:*)` (specs/033-pr-conversation-commands T064, Gate 12 of
  `lint-workflows.yml`): the agent step's `GH_TOKEN` is the App token, which
  per `docs/setup.md` has no Actions permission, and the prompt never
  instructed the agent to use either tool - the same class of defect as
  T065's removal from `pr-conversation.act`.

Sources at the time of writing: `.github/workflows/{intake,clarify,plan,
tasks,implement,finalize,cleanup,rebase,watchdog}.yml` (`claude_args:`
blocks). The composite reads these as literal call-site inputs
(`tool-composition-action.md`), so a disagreement between the live table and
those literals is a documentation bug, not a behavior bug - which is exactly
what Gate 27 now fails on.
