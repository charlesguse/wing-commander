# Contracts: Scratch Spec B

Not applicable. This scratch spec exposes no API, CLI, workflow, or other
external interface (FR-001: the only artifact is `notes.md`). This directory
is present for structural consistency with the planning template and
intentionally contains no contract definitions.
